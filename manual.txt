Geng Liu, April 2nd, 2021
1. Before compiling
   Check input.in file
   Check nx,ny,dim,nq,num_procs in module cart_mpi.f90
   Check geometry initialization in the SetGeometry subroutine in initialization.f90
   
2. COMPILE&RUN with Intel: make (check number of processors & number of threads)

3. Check the host list
    hostfile_mpich

5. Result: output.dat

